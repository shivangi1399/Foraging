# Preprocessing pipeline for ZeroNoise lab

## Installation

From an active conda environment you can install using pip like this:

``` bash
python -m pip install git+http://git.esi.local/havenith-scholvinck-lab/preprocessing.git
```
